[package]
name = "calculateUpLinkDataMIC"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bincode = "1.0"
aes = "0.7.5"
cmac = "0.6.0"
serde = { version = "1.0", features = ["derive"] }
